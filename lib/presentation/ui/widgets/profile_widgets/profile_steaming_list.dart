import 'package:flutter/material.dart';

class ProfileSteamingList extends StatelessWidget {
  const ProfileSteamingList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('No Steaming Videos'),
      ),
    );
  }
}
